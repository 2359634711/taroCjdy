<block wx:if="{{$taroCompReady}}">
    <view class="mainBox" style="{{'max-height:' + maxHeight + 'px;overflow:hidden;'}}">
        <card-item></card-item>
        <card-item></card-item>
        <card-item></card-item>
    </view>
</block>