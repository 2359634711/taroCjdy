<block wx:if="{{$taroCompReady}}">
    <view>
        <swiper indicator-color="#999" indicator-active-color="#333" circular="{{true}}" indicator-dots="{{true}}" autoplay="{{true}}">
            <swiper-item wx:key="val" style="top: 0" wx:for="{{bannerList}}" wx:for-item="val" wx:for-index="_anonIdx2">
                <image mode="aspectFill" src="{{val}}"></image>
            </swiper-item>
        </swiper>
        <info-box compid="{{$compid__10}}"></info-box>
        <refer-box compid="{{$compid__11}}"></refer-box>
        <view class="detailBox">
            <view class="title"></view>
        </view>
        <rich-detail compid="{{$compid__12}}"></rich-detail>
    </view>
</block>