<block wx:if="{{$taroCompReady}}">
    <view class="infoBox">
        <view class="left">
            <view class="fontTitle">{{infoBox.title}}</view>
            <view class="fontInfo">{{infoBox.info}}</view>
        </view>
        <view class="right">
            <image src="{{anonymousState__temp}}"></image>
        </view>
    </view>
</block>